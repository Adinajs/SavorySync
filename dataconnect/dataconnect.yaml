specVersion: "v1"
serviceId: "savorysync"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "savorysync-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
